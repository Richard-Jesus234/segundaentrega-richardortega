---
import { FooterSections } from "../data/infoFooter";
---

<footer class="bg-gray-900 text-gray-300 py-12 px-4 shadow-inner">
  <div class="container mx-auto grid grid-cols-1 md:grid-cols-4 gap-8">
    <div class="col-span-1 flex flex-col items-center md:items-start text-center md:text-left">
      <img src="/src/assets/img/logo2.png" alt="Ratafix Logo" class="w-16 mb-4" />
      <p class="text-sm leading-relaxed mb-4"> Ratafix cuenta con Servicio Técnico para Notebook o Computadores a Domicilio en Castro y comunas cercanas. Nuestro personal Calificado atiende Particulares y Empresas.</p>
    </div>

    {FooterSections.map((section) => (
      <div class="col-span-1 text-center md:text-left">
        <h4 class="text-lg font-semibold text-white mb-4">{section.title}</h4>
          <ul class="space-y-2">
            {section.links.map((link) => (
              <li>
                <a href={link.href} class="text-gray-400 hover:text-green-500 transition duration-300"> {link.label} </a>
              </li>
            ))}
          </ul>
      </div>
    ))}
  </div>

  <!-- derechos reservados con el año que se actualiza automaticamente -->

  <div class="border-t border-gray-700 mt-10 pt-8 text-center text-gray-500 text-sm">
    <p>&copy; {new Date().getFullYear()} Ratafix. Todos los derechos reservados.</p>
  </div>
</footer>